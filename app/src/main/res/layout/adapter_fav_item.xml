<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:paddingBottom="@dimen/dp12"
    android:paddingEnd="@dimen/dp16"
    android:paddingStart="@dimen/dp16"
    android:paddingTop="@dimen/dp12">


    <TextView
        android:id="@+id/fav_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/icons_block"
        android:textSize="16sp" />

    <LinearLayout
        android:id="@+id/icons_block"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/fav_item_poll_icon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_poll_gray_24dp" />

        <ImageView
            android:id="@+id/fav_item_pin_icon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_pin_gray_24dp" />

        <ImageView
            android:id="@+id/fav_item_lock_icon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_lock_black_24dp" />

    </LinearLayout>

    <TextView
        android:id="@+id/fav_item_last_nick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/fav_item_title"
        android:layout_marginTop="4dp"
        android:textColor="@color/text_second_color" />

    <TextView
        android:id="@+id/fav_item_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/fav_item_title"
        android:layout_marginTop="4dp"
        android:textColor="@color/text_second_color" />

</RelativeLayout>